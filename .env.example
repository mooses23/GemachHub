# ===========================================
# REQUIRED ENVIRONMENT VARIABLES
# ===========================================

# Database Configuration (REQUIRED)
# For Neon: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Session Configuration (REQUIRED)
# Generate with: openssl rand -base64 32
SESSION_SECRET="your-secure-random-session-secret-min-32-chars"

# Application Environment
NODE_ENV="production"

# ===========================================
# STRIPE PAYMENT INTEGRATION (REQUIRED for card payments)
# ===========================================

# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_live_your_stripe_secret_key"
STRIPE_PUBLISHABLE_KEY="pk_live_your_stripe_publishable_key"

# Also needed for frontend (Vite prefix)
VITE_STRIPE_PUBLISHABLE_KEY="pk_live_your_stripe_publishable_key"

# Webhook secret from Stripe Dashboard -> Webhooks
STRIPE_WEBHOOK_SECRET="whsec_your_stripe_webhook_secret"

# ===========================================
# PAYPAL PAYMENT INTEGRATION (Optional)
# ===========================================

PAYPAL_CLIENT_ID="your_paypal_client_id"
PAYPAL_CLIENT_SECRET="your_paypal_client_secret"
PAYPAL_MODE="live"

# ===========================================
# EMAIL NOTIFICATIONS (Optional)
# ===========================================

SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your_email@example.com"
SMTP_PASSWORD="your_smtp_password"
SMTP_FROM="noreply@gemachhub.com"
ADMIN_EMAIL="admin@gemachhub.com"

# ===========================================
# APPLICATION SETTINGS
# ===========================================

# Your production URL (for webhooks and redirects)
APP_URL="https://your-domain.vercel.app"

# ===========================================
# MIGRATION NOTES: Replit -> Vercel
# ===========================================
# 
# The following Replit-specific variables are NO LONGER USED:
# - REPLIT_CONNECTORS_HOSTNAME (replaced by STRIPE_SECRET_KEY)
# - REPL_IDENTITY (replaced by STRIPE_SECRET_KEY)
# - WEB_REPL_RENEWAL (replaced by STRIPE_SECRET_KEY)
# - REPLIT_DEPLOYMENT (replaced by NODE_ENV === "production")
# - REPL_ID (only used for development features, safe to ignore)
#
# For Vercel deployment:
# 1. Set all required variables in Vercel Dashboard -> Settings -> Environment Variables
# 2. Enable for Production, Preview, and Development environments
# 3. Redeploy after adding/changing variables
# 4. Configure Stripe webhook endpoint: https://your-domain.vercel.app/api/stripe/webhook
#
