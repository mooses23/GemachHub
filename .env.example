# Database Configuration (REQUIRED)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Session Configuration (REQUIRED)
SESSION_SECRET="your-secure-random-session-secret-min-32-chars"

# Application Environment
NODE_ENV="production"

# Stripe Payment Configuration (REQUIRED for card payments)
STRIPE_SECRET_KEY="sk_live_your_stripe_secret_key"
STRIPE_PUBLISHABLE_KEY="pk_live_your_stripe_publishable_key"
STRIPE_WEBHOOK_SECRET="whsec_your_stripe_webhook_secret"

# PayPal Payment Configuration (REQUIRED for PayPal payments)
PAYPAL_CLIENT_ID="your_paypal_client_id"
PAYPAL_CLIENT_SECRET="your_paypal_client_secret"
PAYPAL_MODE="live"

# Email Configuration (OPTIONAL - for notifications)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your_email@example.com"
SMTP_PASSWORD="your_smtp_password"
SMTP_FROM="noreply@gemachhub.com"

# Application URL (for webhooks and redirects)
APP_URL="https://your-domain.vercel.app"

# Replit-specific (NOT NEEDED FOR VERCEL)
# REPL_ID - Only used for Replit development features
