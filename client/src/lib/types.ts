// Re-export types from shared schema for client use
export type { 
  User,
  Region, 
  Location, 
  GemachApplication, 
  Transaction, 
  Contact,
  Payment,
  PaymentMethod,
  LocationPaymentMethod
} from "@shared/schema";